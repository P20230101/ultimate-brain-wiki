# 终极大脑 Wiki 维护协议

本仓库是一个中文 LLM Wiki / 第二大脑。它遵循 Andrej Karpathy 的 LLM Wiki 模式：原始资料保持不可变，LLM 负责把资料编译成结构化、可追溯、可迭代的 Markdown wiki，人负责选择资料、提出问题和审核关键结论。

## 工作区约定

当前仓库就是 Obsidian 连接的 Second Brain 文件夹。后续所有新增资料、wiki 页面、schema、日志和整理工作，都在这个文件夹内完成。不要另建平行知识库；除非用户明确要求迁移，否则以本目录作为唯一事实工作区。

Obsidian 打开后以 `index.md` 作为工作入口。新笔记默认进入 `wiki/`，附件默认进入 `raw/assets/`，模板存放在 `schema/obsidian-templates/`。

## 三层结构

1. `raw/`：Raw sources，原始资料层。
   - 存放文章、论文、数据、网页剪藏、访谈记录、实验记录、图片和附件。
   - 资料一旦放入后默认不可改。需要修正时新增修正版资料，不覆盖旧文件。
   - LLM 可以读取、引用、摘录和归档，但不能重写原始资料。

2. `wiki/`：The wiki，编译知识层。
   - 存放 LLM 维护的主题页、概念页、方法页、实验页和综合分析页。
   - 这里不是资料堆放区，而是“已处理知识”的工程化表达。
   - 每页必须说明来源、结论、证据、不确定性和下一步可做什么。

3. `AGENTS.md` 与 `schema/`：The schema，维护规则层。
   - `AGENTS.md` 是所有新会话的入口协议。
   - `schema/` 存放页面模板、字段规范和处理流程。
   - 当工作流变得更清楚时，优先更新 schema，再批量整理 wiki。

## 核心文件

- `index.md`：内容索引。按主题列出 wiki 页面、原始资料和当前状态。每次 ingest 或新增分析后更新。
- `log.md`：时间日志。追加记录每次 ingest、query、lint、experiment 和 refactor。不要改写旧日志。
- `README.md`：GitHub 仓库说明。
- `AGENTS/论文写作框架.md`：论文润色、Discussion 写作和论文结构化总结的长期协议。
- `AGENTS/科研工具环境.md`：MinerU 和 Semantic Scholar 的本机配置、变量名和验证方式。

## 长期专业框架

- [论文写作框架](AGENTS/论文写作框架.md)：用于论文润色、Discussion 写作、文献对照和论文 Markdown 总结。
- [科研工具环境](AGENTS/科研工具环境.md)：用于论文解析、OCR、表格/公式提取和 Semantic Scholar 文献检索。

## 页面原则

- 用中文写作。
- 用标准 Markdown 链接，兼容 Obsidian 和 GitHub Pages。
- 不把聊天记录直接倒入 wiki；先抽取为结论、证据、问题和行动项。
- 每个重要结论都要有来源或明确标记为“工作假设”。
- 不为了完整而扩张层级。先保持小而清晰，再按需要增长。
- 发现矛盾时不要静默合并，要在相关页面写出“冲突点”和“待验证项”。

## Ingest 流程

处理新资料时按以下步骤执行：

1. 在 `raw/` 中确认资料路径和资料类型。
2. 阅读资料，提取：核心问题、关键结论、证据、数据、方法、限制、可复用概念。
3. 在 `wiki/` 中创建或更新相关页面。
4. 更新 `index.md` 的目录、状态和来源计数。
5. 在 `log.md` 追加一条日志，格式为：

```md
## [YYYY-MM-DD] ingest | 资料标题

- 来源：`raw/...`
- 更新：`wiki/...`
- 结论：
- 待验证：
```

## Query 流程

回答问题时按以下步骤执行：

1. 先读 `index.md`，定位相关页面。
2. 再读相关 `wiki/` 页面和必要的 `raw/` 资料。
3. 给出有来源的中文答案。
4. 如果答案形成了可复用知识，新增或更新 wiki 页面。
5. 在 `log.md` 追加 `query` 记录。

## Lint 流程

定期检查 wiki 健康度：

- 是否有页面没有被 `index.md` 收录。
- 是否有关键概念被多次提到但没有独立页面。
- 是否有结论和新资料冲突。
- 是否有页面只有摘要、没有证据。
- 是否有待验证项长期未处理。

日志格式：

```md
## [YYYY-MM-DD] lint | 范围

- 发现：
- 修改：
- 下一步：
```

## 工程化要求

- 把每次资料处理看作一次小实验：输入是 raw source，处理是抽取和综合，输出是 wiki 页面。
- 对数据、实验和指标类内容，优先记录变量、方法、样本、结果、误差和限制。
- 对概念和方法类内容，优先记录定义、适用边界、反例、操作步骤和可验证问题。
- 成功标准写清楚：这次更新让哪个问题更容易回答。

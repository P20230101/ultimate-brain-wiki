# PA12 边界载荷方向说明（MatchID 历史格式审计）

本页只保留历史 MatchID `.vfm` 的坐标、机器通道和边界顺序审计；当前 VFM 计算全部由本地自建代码生成。用户确认的机器传感器力直接作为 ROI 边界合力，是自建 VFM 的外载输入约定；它不等于逐帧内外虚功闭合或材料参数识别通过。

## 审计结果

- 结果：**通过**
- `.vfm` 证据：`D:\C盘迁移\Desktop\yuan\data\XY\picture-20250529\vertical_all_45°\PAPER\biaxal\S16_XY_0.2\S16_XY_0.2_258.vfm`
- 中心 ROI 厚度：`1.0 mm`。
- 整体厚度记录：`3.0 mm`。
- Boundary 数量：`4`；Forces 组数：`4`。
- 每组 Forces 帧数：`[258]`。
- Forces 全部为非负值：**是**。
- Forces 首帧全部归零：**是**；后续力值全部为正：**是**。

## 旋转后边界映射

| MatchID Boundary ID | 旋转后边界 | 原机器通道 | 力方向 |
| ---: | --- | --- | --- |
| 0 | 顶部 | X2 | X |
| 1 | 左侧 | Y2 | Y |
| 2 | 右侧 | Y1 | Y |
| 3 | 底部 | X1 | X |

## 使用规则

- 双轴：X 使用顶部/底部两条边，Y 使用左侧/右侧两条边。
- 单轴 X：只使用顶部/底部两条边；单轴 Y：只使用左侧/右侧两条边。
- 拉伸力统一按正值记录；`Press` 的工程单位为 N。
- 外围夹持/加载结构整体厚度为 3 mm；中心 ROI 必须完全位于 1 mm 减薄区，MatchID 工程厚度使用 1 mm。
- 用户确认外围机器力传感器读数可直接作为 ROI 边界合力；自建 VFM 主路径沿用这一外载输入口径，MatchID 文件只作历史方向/格式审计。本项目不恢复四边牵引分布或假设均布边界应力。
- 该边界力规则只规定外部合力输入，不代表逐帧内外虚功已经闭合。S15/S22 的自建 VFM 残差与曲线事件对照见 [GPT/VFM 交接文档](../处理记录/PA12_GPT交接文档.md) 中“曲线事件与自建 VFM 对照”。
- 所有当前双轴试验均为等双轴；按 0.2、2、20 mm/s 分速率识别，不要求非等双轴路径。
- 每个速度先固定 ν=0.375 识别 E，再固定该 E 和 ν 识别屈服 Y 与硬化 H；当前文件审计不代表已得到这些参数。

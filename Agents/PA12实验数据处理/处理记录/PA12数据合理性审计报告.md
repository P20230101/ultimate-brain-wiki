# PA12 数据合理性审计报告

## 审计目的

检查批量生成结果是否满足进入下一阶段 MatchID VFM 前的机器可读契约，不重算原始力值，不替代人工实验判断。

## 总体结论

- 配置实验数：`10`。
- 实验概览行数：`10`。
- MatchID 实验级索引行数：`10`。
- 正式 VFM 三文件审计对象：`8` 个。
- 阻断或待人工确认对象：`2` 个。
- 输出契约审计：**通过**。
- 全部配置实验均已生成正式 VFM：**否**。

## 分实验结果

- `S15_XY_0.2`：照片 `284`，X `284`，Y `284`；三者一致=是。
- `S16_XY_0.2`：照片 `257`，X `257`，Y `257`；三者一致=是。
- `S17_XY_20`：照片 `12`，X `12`，Y `12`；三者一致=是。
- `S18_XY_2`：照片 `126`，X `126`，Y `126`；三者一致=是。
- `S19_X_0.2`：照片 `126`，X `126`，Y `126`；三者一致=是。
- `S20_X_2`：照片 `63`，X `63`，Y `63`；三者一致=是。
- `S21_X_20`：照片 `36`，X `36`，Y `36`；三者一致=是。
- `S22_Y_0.2`：照片 `223`，X `223`，Y `223`；三者一致=是。
- `S23_Y_2`（DATA_LIMITED）：未执行正式 VFM 三文件审计。
- `S24_Y_20`（PRELOAD_RELEASE_ONLY）：未执行正式 VFM 三文件审计。

## 下一步

- 正式 VFM 力值通过数量和数值契约后，仍需确认 MatchID Job 是否覆盖同一批有效帧。
- MatchID 2D 19.2.2.0 的 Results Viewer 字段、分隔符和单位已在当前版本本地交叉验证；更换版本时需重新验证 DAT 映射。
- S23 已由视觉确认断裂，但力文件在断裂前结束，断裂时力缺失；不补造断裂力。S24 是预载释放记录，不进入完整拉伸识别。
- MatchID 自带 VFM 为基准；外围机器力按用户确认直接作为中心 ROI 边界合力。核对中心 ROI 1 mm、外围结构 3 mm、ROI 位置、旋转 XY、逐帧同步和参考帧零力映射。
- 双轴数据均为等双轴；按 0.2、2、20 mm/s 分速率识别。阶段 1 固定 ν=0.375 识别 E；阶段 2 固定 E、ν 识别 Y、H。

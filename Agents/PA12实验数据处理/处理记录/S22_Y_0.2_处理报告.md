# S22_Y_0.2 处理报告

## 结果

- 加载起始力数据索引：`159`，时间 `0.159000 s`。
- 峰值力数据索引：`109550`，时间 `109.708000 s`。
- 自动掉载对应的力数据索引：`178732`，时间 `178.989000 s`。
- 起始照片：`000008.jpg`。
- 断裂照片：`001784.jpg`。
- 有效照片数量：`223`。
- 按 DIC 审计排除的照片帧：`2-7, 9-10`；数量 `8`。
- 排除原因：DAT 质量审计确认这些帧含 <18> 位移记录但没有 <53> 应变记录，不能进入完整 DIC/VFM 场；原始 JPG/DAT 保留不改。。
- 实际照片频率：`9.931220 Hz`。
- 有效实验时间：`178.830000 s`。
- 照片位移（按加载速度×有效时间推算）：`35.766000 mm`。
- 力数据位移（Pos 设备通道）：`36.234400 mm`。
- 应力—应变表：`D:\2026.9.21_finally\Agents\PA12实验数据处理\应力应变\S22_Y_0.2_应力应变.csv`。
- 应力—应变图：`D:\2026.9.21_finally\Agents\PA12实验数据处理\应力应变\S22_Y_0.2_应力应变.png`。

## 同步方法

- 照片时间来源：无可靠 EXIF；按实际帧号间隔归一化到有效力时间区间。
- X/Y 力值：同一组照片时间点分别插值，时间轴完全相同。
- X/Y 通道：X=`X1_Press、X2_Press` 平均，Y=`Y1_Press、Y2_Press` 平均。
- 零点：X 基线 `-8.260000`，Y 基线 `23.300000`；首行校正值强制归零。
- 力符号系数：`-1.0`；按拉伸力正值约定输出，首行校正值强制归零。

## 数量门禁

- 照片数量：`223`。
- X VFM 数量：`223`。
- Y VFM 数量：`223`。
- 正式 VFM 输出：**允许**。
- 配置发布许可：允许。

## 自动检查

- 力时间严格递增：通过。
- 照片时间严格递增：通过。
- X/Y 使用同一时间轴：通过。
- 插值结果含 NaN：否。
- 首行接近零力：通过。
- 校正后 X/Y 力均非负（诊断）：通过。
- 断裂/终点判定：`自动掉载`。
- X1/X2 最大差值：`3.000000`。
- Y1/Y2 最大差值：`23.000000`。
- JPG 缺 DAT：`1592` 个；孤立 DAT：`0` 个。
- JPG 帧号缺口：`[]`；DAT 帧号缺口：`11-15, 17-23, 25-31, 33-39, 41-47, 49-55, 57-63, 65-71, 73-79, 81-87, 89-95, 97-103, …（共 1552 个，222 段）`。
- 有效照片区间内帧号缺口：`9-15, 17-23, 25-31, 33-39, 41-47, 49-55, 57-63, 65-71, 73-79, 81-87, 89-95, 97-103, …（共 1554 个，222 段）`；稀疏序列中的缺口表示未导出的帧，不表示程序补帧失败。
- 位移定义：`two_grip_relative_sum`；当前按两侧位置增量绝对值之和计算相对位移。
- 派生应力—应变位移包络：`启用`；原始 Pos 不修改。
- 非加载方向是否置零：`是`；仅对单轴配置执行，主动方向仍保留校正后的真实力值。
- 应力—应变几何：宽度 `30.0` mm，厚度 `1.0` mm，标距 `30.0` mm。

## 已知限制

- JPG 没有可靠 EXIF 拍摄时间，本次用力数据有效区间和相机设置频率确定首尾候选帧，再按实际帧号间隔将有效区间映射到配对照片。
- 原始 `Press` 通道在本批处理中的加载方向为负；VFM 输出按 `F_corrected = -(F_raw - F_baseline)` 转为拉伸正值。
- 当前名义应力—应变使用 Pos 设备位移；尚未从 DAT 全场字段导出 DIC 位移，因此不能称为 DIC 位移曲线。
- 本阶段已生成名义应力—应变审核结果，但尚未进入 MatchID VFM 识别或材料参数拟合。

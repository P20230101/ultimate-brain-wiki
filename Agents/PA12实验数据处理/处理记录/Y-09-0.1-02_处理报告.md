# Y-09-0.1-02 处理报告

## 结果

- 加载起始力数据索引：`159`，时间 `0.159000 s`。
- 峰值力数据索引：`109550`，时间 `109.708000 s`。
- 断裂掉载力数据索引：`178732`，时间 `178.989000 s`。
- 起始照片：`Img000002.jpg`。
- 断裂照片：`Img001790.jpg`。
- 有效照片数量：`1789`。
- 实际照片频率：`9.998322 Hz`。
- 有效实验时间：`178.830000 s`。
- 照片位移：`17.883000 mm`。
- 力数据位移：`9.055175 mm`。

## 同步方法

- 照片时间来源：无可靠 EXIF；以相机设置频率估计首尾帧，再在有效力时间区间内均匀分配。
- X/Y 力值：同一组照片时间点分别插值，时间轴完全相同。
- X/Y 通道：X=`X1_Press、X2_Press` 平均，Y=`Y1_Press、Y2_Press` 平均。
- 零点：X 基线 `-8.260000`，Y 基线 `23.300000`；首行校正值强制归零。
- 力符号系数：`-1.0`；按拉伸力正值约定输出，首行校正值强制归零。

## 数量门禁

- 照片数量：`1789`。
- X VFM 数量：`1789`。
- Y VFM 数量：`1789`。
- 正式 VFM 输出：**禁止**。
- 配置发布许可：诊断模式，禁止。

## 自动检查

- 力时间严格递增：通过。
- 照片时间严格递增：通过。
- X/Y 使用同一时间轴：通过。
- 插值结果含 NaN：否。
- 首行接近零力：通过。
- 正式 VFM 力值非负：失败。
- 断裂掉载明显：通过。
- X1/X2 最大差值：`3.000000`。
- Y1/Y2 最大差值：`23.000000`。
- JPG 缺 DAT：`0` 个；孤立 DAT：`0` 个。
- JPG 帧号缺口：`[]`；DAT 帧号缺口：`[]`。
- 有效照片区间内帧号缺口：`[]`。

## 已知限制

- JPG 没有可靠 EXIF 拍摄时间，本次用力数据有效区间和相机设置频率确定首尾候选帧，再将有效区间均匀分配到照片。
- 原始 `Press` 通道在本批处理中的加载方向为负；VFM 输出按 `F_corrected = -(F_raw - F_baseline)` 转为拉伸正值。
- 本阶段没有进入应力—应变、VFM 识别或材料参数拟合。

# S19_X_0.2 应力—应变分析

## 计算口径

- 试样有效宽度：`30.000 mm`。
- 中心测量区厚度：`1.000 mm`（论文最终优化参数）。
- 试样总厚度：`3.000 mm`（几何图示推定值，不用于中心名义截面积）。
- 标距：`30.000 mm`（论文中心测量区工作标距）。
- 名义截面积：`30.000 mm²`。
- 名义应力 = 同步力 / 名义截面积；工程应变 = 双侧相对位移 / 标距。
- 工程应变的派生位移使用两侧相对位移的累计加载包络 `maximum.accumulate`；原始 Pos 数据不改写。该规则只适用于当前拉伸至断裂的单调加载段。
- PNG 曲线使用小窗口移动平均仅改善显示连续性；CSV 和同步力值不做平滑改写。
- 该结果用于曲线审核和 MatchID VFM 前置检查，不代表已完成材料参数识别。

## 曲线判断

- X方向：线性拟合斜率 `516.321 MPa`，R²=`0.999482`。
  - 屈服候选：应变 `0.0575233`，应力 `28.0867 MPa`；仅作候选点。
- Y方向：有效点数不足 5，未进行线性段和屈服候选判定。

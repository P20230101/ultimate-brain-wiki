# PA12 自建 VFM 阶段 C：人工复核清单

## 只需确认的三件事

1. **物理 ROI 边界**：S16/S18 的 Job Polygon 是否完全位于中心 1 mm 区域，且 Polygon 外缘是否允许作为 VFM 的物理积分边界。
2. **相关域口径**：DIC subset 中心形成的内缩有效域，是否在本项目方法中被定义为 VFM 有效积分域；如果是，需要给出该规则的来源或实验几何依据。
3. **外功边界**：机器力合力是否仍对应所选积分域的外部边界；若积分域改为内缩域，不能默认沿用完整 ROI 的外功解释。

## 已经确认、无需重复确认

- S16 Job ROI：`312×320 px` Polygon；subset=`15 px`；step=`3 px`。
- 有效点数 `10098=99×102`，面积比恒定 `0.892248`；S18 面积比恒定 `0.891147`。
- 面积偏低的直接数据处理原因是 subset 中心网格内缩，不是逐帧随机丢点。
- S16/S18 总体接近等双轴；H 的漂移不能简单归因于加载比偏离。

## 复核前禁止事项

- 不自动把有效点外接框当作物理 VFM 边界；
- 不把面积门槛改成通过；
- 不发布 S16/S18 的 E/Y/H 为最终材料参数；
- 不把当前候选值写入 FEM 或代理模型标签。

## 复核后的分支

- 若确认 Job Polygon 是物理积分域：保留当前正式口径，面积不足继续标记复核。
- 若确认 DIC 内缩域是项目规定的有效积分域：另做敏感性分析，使用独立配置和独立输出，不覆盖当前结果。
- 若两者均无法确认：维持 `REVIEW_REQUIRED`，转向补充实验几何/方法依据。

## 当前已确认

- 完整 Job ROI 继续作为正式主结果口径。
- DIC subset 内缩有效域已完成独立敏感性分析，输出与主结果隔离；敏感性结果不替代主结果。

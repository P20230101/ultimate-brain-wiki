# PA12 双轴拉伸项目：数据盘点与闭环状态

## 原始资料与输出

- 原始目录：`D:\C盘迁移\Desktop\yuan`
- 第一阶段结构化副本：`D:\C盘迁移\Desktop\yuan\PA12_biaxial_project`
- 当前唯一 Second Brain 工作区：`D:\2026.9.21_finally`
- 旋转 DIC 正式来源：`D:\C盘迁移\Desktop\yuan\data\XY\picture-20250529\vertical_all_45°\PAPER`
- 批量处理配置：`configs/pa12_rotated_batch.json`
- 当前机器清单：`Agents/PA12实验数据处理/处理记录/PA12批量处理清单.json`
- 当前审计结果：`Agents/PA12实验数据处理/处理记录/PA12数据合理性审计结果.json`

## 第一阶段盘点结论

- 原始目录包含 JPG、DAT、XLS、PDF 和 MatchID 工程文件；原始资料保持只读。
- 状态导出工作表包括 `Pos`、`Speed`、`Press`；`Press` 中的 `T`、四个 Press 通道和 `Pos` 位移时序可读取，时间间隔约 1 ms。
- 用户已确认 `Press` 是力；当前默认使用同方向双传感器平均值。
- `orginal_all` 仅用于早期样例验证，不是当前闭环的最终图像来源。
- 当前最终图像来源固定为 `vertical_all_45°/PAPER`；`CHECK` 不作为正式输入。

## 旋转序列配对结论

| 序列 | JPG | DAT | JPG/DAT 配对 | 说明 |
| --- | ---: | ---: | ---: | --- |
| S15 | 289 | 286 | 286 | 末尾 3 张无 DAT |
| S16 | 259 | 258 | 258 | 末尾 1 张无 DAT |
| S17 | 21 | 20 | 20 | 末尾 1 张无 DAT |
| S18 | 267 | 249 | 249 | 末尾 18 张无 DAT |
| S19 | 130 | 128 | 128 | 末尾 2 张无 DAT |
| S20 | 133 | 133 | 133 | 完整配对 |
| S21 | 81 | 80 | 80 | 末尾 1 张无 DAT |
| S22 | 1825 | 233 | 233 | DAT 稀疏输出，约每 8 个 JPG 帧保留一组 |
| S23 | 1558 | 194 | 194 | DAT 稀疏输出，约每 8 个 JPG 帧保留一组 |
| S24 | 711 | 157 | 157 | DAT 稀疏输出，约每 4 个 JPG 帧保留一组 |

正式索引规则是同名 JPG+DAT 配对集合。无 DAT 的 JPG 不删除、不补齐；有效区间缺配对帧时阻断正式输出。

## 当前闭环状态

### 已完成

- 固定十组旋转序列—力文件映射。
- 读取 `Press` 力和 `Pos` 位移。
- 以加载前基线校正力值，并按用户确认的等双轴规则合成 X/Y。
- 自动检测加载起点、峰值和明显掉载点；支持 JSON 人工索引覆盖。
- 以配对 JPG/DAT 为照片主索引，使用同一照片时间轴插值 X/Y。
- 生成 8 组通过正式同步力值门禁的 X/Y 单列 CSV。
- 生成 S15—S23 的照片—力表、检查图和名义应力—应变审核结果；S24 尚未进入事件同步。
- 生成十组 Job/DAT 元数据和已处理实验的 MatchID 帧—力—时间索引。
- 对 1327 个选定帧完成 DAT 记录质量审计：1319 个含 `<18>/<53>`，S22 有 8 个 `NO_STRAIN_RECORDS`；S15 的末帧 DAT 有记录但当前 Job 未列入。
- 生成汇总概览 CSV/XLSX、批量清单、审计报告和 GPT 交接文档。

### 仍未完成

- S23 没有记录到明显掉载，末尾只是力文件记录终点。
- S24 开头已有约 1555 N 预载，无法从当前文件独立估计零点和加载起点。
- S15 的 Job 缺 1 个有效帧但对应 DAT 可读；S22 的 Job 缺 8 个有效帧且对应 DAT 没有 `<53>` 记录。
- DAT `<18>`、`<53>` 的逐点字段映射、Results Viewer 导出列名、分隔符和单位已由 MatchID 2D 19.2.2.0 同帧交叉验证；这不是官方 DAT 格式文档，换版本时仍需重验。
- 本机 Python 依赖和原始输入均可用，已发现 `D:\DIC\MATCH_DIC\2019\MatchID 2D\MatchID.exe`（19.2.2.0）；当前剩余工作是各实验导出/重构和闭环门禁，不是重新猜测字段含义。
- MatchID VFM 边界力、载荷分布、几何、厚度、坐标变换和材料参数识别未完成。

## 重要工作假设

- `Press` 为力，工程单位按用户确认的 `N` 使用；后续处理不再把该单位列为未知。
- `X=(X1_Press+X2_Press)/2`，`Y=(Y1_Press+Y2_Press)/2`。
- 当前拉伸正力输出为 `F_corrected=-(F_raw-F_baseline)`。
- 中心名义应力审核使用宽度 30 mm、中心厚度 1 mm、标距 30 mm，截面积 30 mm²；总厚度 3 mm 不用于该截面积。
- 论文中的 Abaqus 参数是优化阶段工作参数，不可直接称为实验识别结果。

## 下一步

1. 读取 `Agents/PA12实验数据处理/处理记录/PA12_GPT交接文档.md` 和审计 JSON。
2. 先按 `MatchID_VFM准备/PA12_MatchID_VFM闭环状态.md` 处理 S15/S22 的 Job/DAT 差异；沿用已验证的字段映射，换 MatchID 版本时再做同帧复核。
3. 填写 `configs/matchid_export_example.json` 的字段映射和单位，批量导出 DIC 全场数据并与帧—力—时间索引合并。
4. 确认边界载荷、几何和单位后进入 MatchID VFM，再进行材料参数识别。

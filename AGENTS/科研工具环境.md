# 科研工具环境

本文件记录论文阅读、解析和文献检索所依赖的本机工具。密钥只保存在 Windows 用户级环境变量和对应工具的本机配置中，不写入 Second Brain 或 Git 仓库。

## MinerU

- 来源：`opendatalab/MinerU` 的 `mineru` skill。
- Agent skill：`C:/Users/Administrator/.agents/skills/mineru/SKILL.md`。
- MinerU 版本：`4.0.5`。
- Python：`3.12.10`。
- 标准模型：`MinerU-4_models_torch` 与 `MinerU2.5-Pro-2605-1.2B-GGUF` 已下载并校验。
- 本地硬件：NVIDIA RTX 5060，CUDA 12.8，`torch 2.8.0+cu128`，CUDA 可用。
- 本地后端：用户级变量 `MINERU_MODEL_SMALL_BACKEND=torch`、`MINERU_MODEL_VLM_ENGINE=llama-cpp`。
- 本地服务：`parse_server.local.mode=managed`，档位为 `standard`，当前已通过健康检查。
- 兼容依赖：`transformers 5.10.1`、`huggingface-hub 1.5.0`；`python -m pip check` 无断裂依赖。
- 远程端点：`https://mineru.net/api`。
- 远程配置变量：`MINERU_API_KEY`。
- `paper-analyzer` 兼容变量：`MINERU_TOKEN`、`MINERU_API_TOKEN`。
- 远程状态：健康，`mineru usage --json` 已返回账户用量和限制信息。
- 本地验收：PA12 检查图已用 `standard` 档解析成功，locator 为 `doc:0b6e14f/tier:standard/page:1`。
- 新文件验收：PA12 `X-0.1-258.csv` 首次解析成功，`cache_hit=false`、`status=done`、`tier=flash`，locator 为 `doc:8152268/tier:flash/page:1/block:1`。
- 已知状态：ONNX 模型仓库仍不完整，但不再是当前后端；`auto` 只在新进程未继承用户变量时显示为默认值。

常用命令：

```powershell
mineru version --json
mineru server start
mineru server status --json
mineru parse "论文.pdf" --json
mineru usage --json
```

MinerU 默认遵守本地优先和隐私边界。使用远程解析时，必须明确指定远程路径或由用户授权的工作流调用远程服务。

当前 Codex 进程不会自动刷新运行期间新增的 Windows 用户级变量。写入变量后重启 Codex 或新建终端，再运行 `mineru-kit models show`，应看到 `torch` 与 `llama-cpp`。

## Abaqus

- 版本：Abaqus 2025。
- 命令入口：`D:\SIMULIA\Commands\abaqus.bat`。
- 运行边界：Abaqus 2025 内置 Python 对中文路径存在代码页兼容问题；`AGENTS/PA12双轴试样仿真/run_scan.ps1` 会使用纯 ASCII 临时路径调用 Abaqus，输入和结果仍归档到本 vault 的 `runs/`。
- 前处理验证：五个真实 PA12 STEP 均生成 `.inp`、`.cae` 和 `run_manifest.json`；每个输入含全局 `X/Y/Z` 材料方向与 `CENTER_REGION` 集合。
- 求解验证：`a=3.0 mm` 粗网格流程临时材料值 smoke solve 已生成 ODB，`.sta` 报告 `THE ANALYSIS HAS COMPLETED SUCCESSFULLY`，`.dat` 未发现 fatal/error。
- 后处理验证：中心区 `S11/S22/E11/E22` 已输出变异系数、`K_ratio` 和 `primary_score`。
- 科学边界：当前材料参数只用于工具链验证，不能用于正式 PA12 结论；正式仿真必须替换为 X/Y/Z 独立试验标定值，并记录单位、加载比和网格收敛。

## Semantic Scholar

- API 端点：`https://api.semanticscholar.org/graph/v1`。
- 用户级环境变量：`S2_API_KEY`、`SEMANTIC_SCHOLAR_API_KEY`。
- 请求认证头：`x-api-key`。
- 验证方式：论文搜索接口返回 `HTTP 200` 和论文元数据。
- 当前状态：变量已被当前进程读取；最近复测返回 `HTTP 429 Too Many Requests`，属于服务端限流，等待限流窗口后再复测，不在本机添加重试循环或替代接口。

示例请求：

```powershell
$headers = @{ "x-api-key" = $env:S2_API_KEY }
Invoke-RestMethod `
  -Uri "https://api.semanticscholar.org/graph/v1/paper/search?query=machine%20learning&limit=1&fields=title,year" `
  -Headers $headers
```

## 生效方式

API key 使用 Windows 用户级环境变量保存。重启 Codex 或新建终端后，Agent 和脚本才能自动读取最新变量；当前会话中需要显式从用户环境加载变量后再运行脚本。

密钥不写入 Second Brain、日志或 Git；环境记录只保留变量名、端点和验证结果。
